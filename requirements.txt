# Step 1: Install CUDA PyTorch explicitly
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Step 2: Install remaining dependencies
# pip install -r requirements.txt

# check PyTorch WITH CUDA support is installed
# pip show torch

# Run in terminal for Avlibale GPU
# nvidia-smi

# uvicorn main:app
# http://127.0.0.1:8000/docs

#⏳ First run may take 30–60 seconds (model load)
# After that → responses will be fast ⚡ 

# ===============================
# Backend Framework
# ===============================
fastapi==0.110.0
uvicorn[standard]==0.29.0

# ===============================
# AI / Machine Learning
# ===============================
# PyTorch (CUDA-enabled build must be installed separately)
torch==2.7.1+cu118

transformers>=4.39.0
accelerate>=0.27.0
sentencepiece>=0.1.99

# ===============================
# Data & Validation
# ===============================
pydantic>=2.6.0
numpy>=1.24.0
